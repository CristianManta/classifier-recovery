_target_: dem.energies.classifier_energy.Classifier

dimensionality: 100 # Change this depending on the dataset
cls: 0 # class from which we want to generate samples
resize_shape: 10 # Size to rescale the images to
energy_type: "log_prob" # Choices: 'log_prob' or 'logit'
n_mixes: 1
loc_scaling: 40
log_var_scaling: 1.0

device: ${trainer.accelerator}

plot_samples_epoch_period: 1

should_unnormalize: false
data_normalization_factor: 50
